<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ymt.demo1">

    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!--
 To retrieve OAuth 2.0 tokens or invalidate tokens to disconnect a user. This disconnect
     option is required to comply with the Google+ Sign-In developer policies
    -->
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />
    <!-- To retrieve the account name (email) as part of sign-in: -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        tools:replace="android:icon">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity android:name=".guidepages.GuideActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CircleMenuActivity"
            android:launchMode="singleTask" />
        <activity android:name=".mainTabPages.sign.SignUpActivity" />
        <activity android:name=".mainTabPages.advice.AdviceActivity" />
        <activity android:name=".mainTabPages.help.HelpActivity" />
        <activity android:name=".mainTabPages.SignInActivity" />
        <activity android:name=".mainTabPages.setting.SettingActivity" />
        <activity android:name=".mainTabPages.sign.FoundPswActivity" />
        <activity android:name=".mainTabPages.setting.AboutUsActivity" />
        <activity android:name=".mainTabPages.setting.ManageStoreActivity" />
        <activity android:name=".mainTabPages.setting.ManageAccountActivity" />
        <activity android:name=".mainTabPages.setting.ManageAppearanceActivity" />
        <activity android:name=".mainTabPages.setting.ManagePswActivity" />
        <activity android:name=".mainCirclePages.PersonalPagerTabAcvitiy" />
        <activity android:name=".mainCirclePages.MoreCatoActivity" />


    </application>

</manifest>
